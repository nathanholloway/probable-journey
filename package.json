{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "name": "@nathanholloway/probable-journey",
  "devDependencies": {
    "lerna": "^6.4.1"
  },
  "scripts": {
    "build": "lerna run build",
    "publish": "lerna run publish",
    "version": "lerna version --conventional-commits --yes"
  }
}
